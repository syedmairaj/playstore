/**
 * Stored in `keyword_rank_snapshots.rank` when the app is not in the first page
 * of organic Play results returned by Serper (`num: 20`). Shared by server routes
 * and the Keyword Tracker UI for consistent labeling.
 */
export const SERPER_RANK_NOT_IN_FIRST_PAGE = 101;

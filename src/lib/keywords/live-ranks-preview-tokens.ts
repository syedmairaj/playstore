/**
 * Client- and server-safe tokens for live rank preview errors (`results[].error` from
 * `POST /api/serper/play-store-search`). Map in UI to translated copy — do not show raw HTTP text.
 */
export const LIVE_RANKS_PREVIEW_UNAVAILABLE = "LIVE_RANKS_PREVIEW_UNAVAILABLE";

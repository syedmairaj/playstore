/**
 * @deprecated Canonical implementation lives in `src/components/keyword-tracker/KeywordTrackerClient.tsx`.
 * App routes import via `@/components/...` (tsconfig → `src/`). This re-export avoids drift from a duplicate copy.
 */
export { KeywordTrackerClient } from "@/components/keyword-tracker/KeywordTrackerClient";

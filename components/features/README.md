# `components/features`

Feature-oriented UI that pairs with `lib/features/product/<name>/`.

- **`aso/`** — Workspace ASO health panel (`AsoScorePanel`).
- **Future:** `ranking-tracker/`, `alerts-advanced/`, etc.

Marketing blocks stay under `components/marketing/`.

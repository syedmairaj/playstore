╔════════════════════════════════════════════════════════════════════════════╗
║                 BRAND MIRROR ENGINE - QUICK REFERENCE                      ║
║                          For Lead Architect Review                         ║
╚════════════════════════════════════════════════════════════════════════════╝

PROBLEM SOLVED:
  ├─ 22P02: invalid input syntax for type json
  ├─ NULL competitor_id in database  
  ├─ "No keywords found" errors
  └─ Silent failures in keyword retrieval

SOLUTION IMPLEMENTED:
  ├─ Strict schema enforcement (BRAND-MIRROR-ENGINE-SCHEMA.ts)
  ├─ Correct serialization (metadata as native object)
  ├─ Comprehensive debug logging (at INSERT and SELECT)
  └─ Consistency checking (signal_type, language, competitor_id)

════════════════════════════════════════════════════════════════════════════════

SCHEMA ENFORCEMENT
─────────────────────────────────────────────────────────────────────────────

File: /lib/staging-vault/BRAND-MIRROR-ENGINE-SCHEMA.ts

✓ Interface: CompetitorWeaknessSignalSchema
✓ Interface: CompetitorWeaknessMetadata  
✓ Function: validateCompetitorWeaknessSchema(data)
✓ Function: buildCompetitorWeaknessSchema(input)
✓ Function: buildAndValidateSchema(input)

ENFORCES:
  • metadata: NATIVE OBJECT (not stringified)
  • content: JSON STRING (stringified)
  • signal_type: 'competitor_weakness' (exact match)
  • language: 'en' | 'ar' (no other values)
  • competitor_id: REQUIRED (used in SELECT)
  • keywords_by_strategy: { high_volume, intent_based, competitor_gap }

════════════════════════════════════════════════════════════════════════════════

DATABASE OPERATIONS
─────────────────────────────────────────────────────────────────────────────

INSERT (/lib/staging-vault/staging-vault-service.ts)
  • Validates schema BEFORE sending to database
  • Logs: [StagingVault] Metadata validation passed
  • Logs: [StagingVault] Inserting signal: metadataType: object
  • Metadata passed as NATIVE OBJECT (Supabase handles serialization)

SELECT (/app/api/competitors/[id]/keywords/route.ts)
  • Logs query parameters BEFORE executing
  • Logs: [CompetitorKeywords] Executing query: { signal_type, competitor_id, language }
  • If found: [CompetitorKeywords] ✓ SIGNAL FOUND
  • If not found: [CompetitorKeywords] ❌ NO SIGNALS FOUND (with diagnostics)
  • If error: [CompetitorKeywords] ❌ DATABASE ERROR

════════════════════════════════════════════════════════════════════════════════

LANGUAGE SUPPORT
─────────────────────────────────────────────────────────────────────────────

English (EN)
  ├─ Stored as: language = 'en'
  ├─ metadata.is_rtl = false
  └─ Keywords: "fitness tracker", "calorie counter", etc.

Arabic (AR)
  ├─ Stored as: language = 'ar'
  ├─ metadata.is_rtl = true
  └─ Keywords: "متتبع اللياقة", "عداد السعرات", etc.

ISOLATION:
  • EN and AR stored in separate rows
  • SELECT filters by language parameter
  • No language mixing (EN won't appear in AR result)

════════════════════════════════════════════════════════════════════════════════

TESTING CHECKLIST
─────────────────────────────────────────────────────────────────────────────

✓ English Keywords (EN)
  1. Analyze competitor in English
  2. Server logs: ✓ Metadata validation passed
  3. Server logs: ✓ SIGNAL FOUND
  4. UI displays: Keywords in English

✓ Arabic Keywords (AR)
  1. Switch language to Arabic
  2. Analyze same competitor
  3. Server logs: ✓ SIGNAL FOUND (with language: ar)
  4. UI displays: Keywords in Arabic (different from EN)

✓ Database Verification
  Run SQL:
    SELECT metadata->>'competitor_id', language, count(*)
    FROM workspace_staging_vault
    WHERE signal_type = 'competitor_weakness'
    GROUP BY metadata->>'competitor_id', language;
  
  Expected:
    • competitor_id NOT NULL
    • Both 'en' and 'ar' rows
    • count > 0

════════════════════════════════════════════════════════════════════════════════

IF TESTS FAIL
─────────────────────────────────────────────────────────────────────────────

Server Logs:
  [StagingVault] ← INSERT issues
  [CompetitorKeywords] ← SELECT issues
  
  Read both messages for exact problem

Read detailed docs:
  /BRAND-MIRROR-ENGINE-SERIALIZATION-AUDIT.md
  /READY_FOR_PRODUCTION_DEPLOYMENT.md

════════════════════════════════════════════════════════════════════════════════

FILES REFERENCE
─────────────────────────────────────────────────────────────────────────────

SCHEMA DEFINITION:
  /lib/staging-vault/BRAND-MIRROR-ENGINE-SCHEMA.ts

SERIALIZATION FIX:
  /lib/staging-vault/staging-vault-service.ts

DEBUG LOGGING:
  /app/api/workspaces/[workspaceId]/competitors/[competitorId]/keywords/route.ts

DOCUMENTATION:
  /BRAND-MIRROR-ENGINE-SERIALIZATION-AUDIT.md
  /READY_FOR_PRODUCTION_DEPLOYMENT.md
  /IMPLEMENTATION_SUMMARY_FOR_TESTING.txt

════════════════════════════════════════════════════════════════════════════════

DEPLOYMENT STATUS: ✅ READY FOR PRODUCTION

Next step: Run tests above to confirm fix works.

════════════════════════════════════════════════════════════════════════════════
